<Window x:Class="Lab2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab2"
        mc:Ignorable="d"
        Title="TextEditor" Height="450" Width="600">

    <!-- Головний контейнер -->
    <Grid>
        <!-- Визначення двох рядків: 
             1) автоматична висота для меню 
             2) решта простору для тексту -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Рамка (Border) з заокругленими кутами у першому рядку -->
        <Border Grid.Row="0"
                BorderBrush="#FFCBBEBE"
                BorderThickness="2"
                CornerRadius="10">
            <!-- Горизонтальний StackPanel із чотирма кнопками -->
            <StackPanel Orientation="Horizontal">
                <!-- Кнопка "Відкрити" -->
                <Button ToolTip="Відкрити" >
                    <Image Source="images/open.png" Width="40" Height="40"/>
                </Button>
                <!-- Кнопка "Стерти" -->
                <Button ToolTip="Стерти" >
                    <Image Source="images/clean.png" Width="40" Height="40"/>
                </Button>
                <!-- Кнопка "Копіювати" -->
                <Button ToolTip="Копіювати" >
                    <Image Source="images/copy.png" Width="40" Height="40"/>
                </Button>
                <!-- Кнопка "Вставити" -->
                <Button ToolTip="Вставити">
                    <Image Source="images/paste.png" Width="40" Height="40"/>
                </Button>
            </StackPanel>
        </Border>

        <!-- Текстова область (другий рядок Grid) -->
        <TextBox Grid.Row="1"
                 Margin="5"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>
    </Grid>
</Window>